# SO Draft Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ให้ผู้ใช้บันทึก/กู้คืน Sales Order ที่ยังกรอกไม่เสร็จได้ ทั้ง auto-save ลงเครื่อง (กันข้อมูลหายตอนปิด/ออกโปรแกรม) และบันทึกเป็น "ร่าง" (draft record) ที่กลับมาทำต่อ/โปรโมตเป็น SO จริงได้

**Architecture:** แยก logic ที่ test ได้ออกเป็น pure helpers ใน `helpers.ts` (มี unit test) แล้ว wire เข้า `Sales.jsx` (UI verify ด้วยตา + typecheck) Draft เป็น record ใน array `sales` เดิม (persist/sync ตามปกติ) ด้วย `status:"draft"`, `soNum:""` จึงไม่กินเลข ไม่จองสต็อก ไม่เข้ารายงาน

**Tech Stack:** React (JSX), TypeScript helpers, Vitest, localStorage, Vite

**Spec:** `docs/superpowers/specs/2026-06-30-so-draft-design.md`

**ข้อควรระวัง (จาก memory):**
- record id ใหม่ต้องใช้ `Date.now()+Math.random()` (กัน id ชนกันใน StrictMode) — ห้ามใช้ `Date.now()` เปล่า
- ห้ามเรียก setState ซ้อนใน updater ของ setState อื่น
- import TS util ใน `Sales.jsx`/test ใช้นามสกุล `.js` (ตามที่ไฟล์เดิมทำ เช่น `from "../utils/helpers.js"`)
- **การ verify ที่เขียนลง Supabase (กดบันทึกร่าง/โปรโมต) แตะ prod DB จริง** — อย่ากดเองตอน verify ถ้าไม่ได้รับอนุญาตจากผู้ใช้ ใช้ typecheck + unit test + ตรวจด้วยตาแทน ส่วน autosave (localStorage) verify ใน preview ได้ปลอดภัย

---

## File Structure

- `src/utils/constants.js` — เพิ่มค่าคงที่ `SO_AUTOSAVE_KEY`
- `src/utils/helpers.ts` — เพิ่ม pure helpers: `soFormHasContent`, `parseSoAutosave`, `resolveSaveSoNum`, `resolveSaveStatus`, `draftFromForm`
- `src/utils/helpers.test.ts` — เพิ่ม test ของ helpers ใหม่
- `src/components/ui/Badge.jsx` — เปลี่ยน label `draft` เป็น "ร่าง"
- `src/components/ui/Modal.tsx` — ขยาย `MBtns` รองรับปุ่ม secondary + flex-wrap (มือถือ)
- `src/components/Sales.jsx` — wire autosave, saveDraft, ปุ่ม, refactor `doSave` (promote), filter tab "ร่าง", row draft, exclude draft จาก stats

---

## Task 1: Helper `soFormHasContent`

ใช้ตัดสินว่าฟอร์ม/ร่าง "มีเนื้อ" พอจะกู้คืน/บันทึกร่างไหม (มีลูกค้า หรือมีสินค้าอย่างน้อย 1 รายการ)

**Files:**
- Modify: `src/utils/helpers.ts` (เพิ่มท้ายไฟล์)
- Test: `src/utils/helpers.test.ts`

- [ ] **Step 1: เขียน test ที่ fail**

เพิ่มใน `src/utils/helpers.test.ts` (เพิ่มชื่อ `soFormHasContent` ใน import block จาก `"./helpers.js"` ด้วย):

```ts
describe("soFormHasContent", () => {
  test("ฟอร์มเปล่า -> false", () => {
    expect(soFormHasContent({ customerId: "", items: [{ productId: "" }] })).toBe(false);
  });
  test("เลือกลูกค้าแล้ว -> true", () => {
    expect(soFormHasContent({ customerId: "5", items: [{ productId: "" }] })).toBe(true);
  });
  test("มีสินค้าอย่างน้อย 1 -> true", () => {
    expect(soFormHasContent({ customerId: "", items: [{ productId: "3" }] })).toBe(true);
  });
  test("null/undefined -> false", () => {
    expect(soFormHasContent(null as never)).toBe(false);
    expect(soFormHasContent({} as never)).toBe(false);
  });
});
```

- [ ] **Step 2: รัน test ให้เห็นว่า fail**

Run: `npm test -- helpers`
Expected: FAIL — `soFormHasContent is not a function` / import error

- [ ] **Step 3: เขียน implementation**

เพิ่มท้าย `src/utils/helpers.ts`:

```ts
export const soFormHasContent = (form: {
  customerId?: unknown;
  items?: Array<{ productId?: unknown } | null>;
}): boolean => {
  if (!form) return false;
  if (form.customerId) return true;
  return (form.items || []).some((it) => !!it && !!it.productId);
};
```

- [ ] **Step 4: รัน test ให้ผ่าน**

Run: `npm test -- helpers`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/utils/helpers.ts src/utils/helpers.test.ts
git commit -m "feat(sales): soFormHasContent helper for SO draft"
```

---

## Task 2: Helper `parseSoAutosave`

อ่าน autosave จาก localStorage แบบ defensive — parse ไม่ได้/ผิด schema คืน null ไม่ throw

**Files:**
- Modify: `src/utils/helpers.ts`
- Test: `src/utils/helpers.test.ts`

- [ ] **Step 1: เขียน test ที่ fail**

เพิ่มใน `helpers.test.ts` (เพิ่ม `parseSoAutosave` ใน import):

```ts
describe("parseSoAutosave", () => {
  test("null/empty -> null", () => {
    expect(parseSoAutosave(null)).toBe(null);
    expect(parseSoAutosave("")).toBe(null);
  });
  test("JSON พัง -> null", () => {
    expect(parseSoAutosave("{not json")).toBe(null);
  });
  test("ไม่มี form -> null", () => {
    expect(parseSoAutosave("{}")).toBe(null);
    expect(parseSoAutosave('{"form":123}')).toBe(null);
  });
  test("มี form object -> คืน object", () => {
    const r = parseSoAutosave('{"form":{"customerId":"5"},"payType":"cash"}');
    expect(r).not.toBe(null);
    expect((r as { form: { customerId: string } }).form.customerId).toBe("5");
  });
});
```

- [ ] **Step 2: รัน test ให้ fail**

Run: `npm test -- helpers`
Expected: FAIL — `parseSoAutosave is not a function`

- [ ] **Step 3: เขียน implementation**

เพิ่มท้าย `helpers.ts`:

```ts
export const parseSoAutosave = (
  raw: string | null
): null | { form: Record<string, unknown>; [k: string]: unknown } => {
  if (!raw) return null;
  try {
    const o = JSON.parse(raw);
    if (!o || typeof o !== "object" || !o.form || typeof o.form !== "object") return null;
    return o;
  } catch {
    return null;
  }
};
```

- [ ] **Step 4: รัน test ให้ผ่าน**

Run: `npm test -- helpers`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/utils/helpers.ts src/utils/helpers.test.ts
git commit -m "feat(sales): parseSoAutosave defensive parser"
```

---

## Task 3: Helpers `resolveSaveSoNum` + `resolveSaveStatus`

แยก logic การตัดสินเลข SO และสถานะตอนเซฟ ให้รองรับ 3 กรณี: สร้างใหม่ / แก้ SO จริงเดิม / โปรโมตร่าง

**Files:**
- Modify: `src/utils/helpers.ts`
- Test: `src/utils/helpers.test.ts`

- [ ] **Step 1: เขียน test ที่ fail**

เพิ่มใน `helpers.test.ts` (เพิ่ม `resolveSaveSoNum, resolveSaveStatus` ใน import; `nextDocNum` มี import อยู่แล้ว):

```ts
describe("resolveSaveSoNum", () => {
  test("สร้างใหม่ (oldSO null) -> เลขใหม่ขึ้นต้น SO-", () => {
    expect(resolveSaveSoNum(null, []).startsWith("SO-")).toBe(true);
  });
  test("โปรโมตร่าง -> เลขใหม่ (ไม่ใช่ค่าว่างของร่าง)", () => {
    const r = resolveSaveSoNum({ status: "draft", soNum: "" }, []);
    expect(r.startsWith("SO-")).toBe(true);
  });
  test("แก้ SO จริงเดิม -> คงเลขเดิม", () => {
    expect(resolveSaveSoNum({ status: "pending_delivery", soNum: "SO-2026-06-001" }, [])).toBe(
      "SO-2026-06-001"
    );
  });
});

describe("resolveSaveStatus", () => {
  test("ใหม่ ไม่ต้องอนุมัติ -> pending_delivery", () => {
    expect(resolveSaveStatus(null, false)).toBe("pending_delivery");
  });
  test("ใหม่ ต้องอนุมัติ -> pending_special_approval", () => {
    expect(resolveSaveStatus(null, true)).toBe("pending_special_approval");
  });
  test("โปรโมตร่าง ต้องอนุมัติ -> pending_special_approval", () => {
    expect(resolveSaveStatus({ status: "draft" }, true)).toBe("pending_special_approval");
  });
  test("โปรโมตร่าง ไม่ต้องอนุมัติ -> pending_delivery", () => {
    expect(resolveSaveStatus({ status: "draft" }, false)).toBe("pending_delivery");
  });
  test("แก้ SO เดิม ไม่ต้องอนุมัติ -> คงสถานะเดิม", () => {
    expect(resolveSaveStatus({ status: "out_for_delivery" }, false)).toBe("out_for_delivery");
  });
  test("แก้ SO เดิม ต้องอนุมัติ -> pending_special_approval", () => {
    expect(resolveSaveStatus({ status: "completed" }, true)).toBe("pending_special_approval");
  });
});
```

- [ ] **Step 2: รัน test ให้ fail**

Run: `npm test -- helpers`
Expected: FAIL — functions not defined

- [ ] **Step 3: เขียน implementation**

เพิ่มท้าย `helpers.ts` (`nextDocNum` อยู่ในไฟล์เดียวกันแล้ว เรียกตรงได้):

```ts
export const resolveSaveSoNum = (
  oldSO: { status?: string; soNum?: string } | null | undefined,
  sales: Array<{ [k: string]: unknown }>
): string => {
  if (!oldSO || oldSO.status === "draft") return nextDocNum("SO", sales, "soNum");
  return oldSO.soNum || "";
};

export const resolveSaveStatus = (
  oldSO: { status?: string } | null | undefined,
  needsApproval: boolean
): string => {
  if (!oldSO || oldSO.status === "draft") {
    return needsApproval ? "pending_special_approval" : "pending_delivery";
  }
  if (needsApproval) return "pending_special_approval";
  return oldSO.status || "pending_delivery";
};
```

- [ ] **Step 4: รัน test ให้ผ่าน**

Run: `npm test -- helpers`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/utils/helpers.ts src/utils/helpers.test.ts
git commit -m "feat(sales): resolveSaveSoNum/resolveSaveStatus for draft promote"
```

---

## Task 4: Helper `draftFromForm`

แปลง state ฟอร์ม + ui scalars เป็นฟิลด์ของ draft record ที่ `openEdit` อ่านกลับได้ (เก็บเป็น type เดียวกับ SO จริง: customerId เป็นตัวเลข, items productId เป็นตัวเลข)

**Files:**
- Modify: `src/utils/helpers.ts`
- Test: `src/utils/helpers.test.ts`

- [ ] **Step 1: เขียน test ที่ fail**

เพิ่มใน `helpers.test.ts` (เพิ่ม `draftFromForm` ใน import):

```ts
describe("draftFromForm", () => {
  const ui = {
    incVat: true,
    payType: "cash",
    discPct: 1,
    creditDays: 45,
    extraDiscPct: "",
    extraDiscAmt: "",
    vatRepName: "",
    vatRepAddress: "",
    vatRepIdCard: "",
  };
  test("แปลง customerId/items เป็นตัวเลข", () => {
    const r = draftFromForm(
      { customerId: "5", date: "2026-06-30", items: [{ productId: "3", qty: "2", price: "100" }] },
      ui
    );
    expect(r.customerId).toBe(5);
    expect(r.items).toEqual([{ productId: 3, qty: 2, price: 100 }]);
    expect(r.includeVat).toBe(true);
    expect(r.payType).toBe("cash");
  });
  test("กรองรายการสินค้าที่ยังไม่เลือกทิ้ง", () => {
    const r = draftFromForm(
      { customerId: "5", items: [{ productId: "3", qty: 1, price: 0 }, { productId: "", qty: 1, price: 0 }] },
      ui
    );
    expect(r.items).toHaveLength(1);
  });
  test("ไม่เลือกลูกค้า -> customerId คงเป็นค่าว่าง", () => {
    const r = draftFromForm({ customerId: "", items: [{ productId: "3", qty: 1, price: 0 }] }, ui);
    expect(r.customerId).toBe("");
  });
});
```

- [ ] **Step 2: รัน test ให้ fail**

Run: `npm test -- helpers`
Expected: FAIL — `draftFromForm is not a function`

- [ ] **Step 3: เขียน implementation**

เพิ่มท้าย `helpers.ts`:

```ts
export const draftFromForm = (
  form: {
    customerId?: string | number;
    date?: string;
    items?: Array<{ productId?: string | number; qty?: string | number; price?: string | number }>;
    useVatRep?: boolean;
    note?: string;
    legacyNum?: string;
    eventId?: string;
    eventPackPurchases?: unknown[];
  },
  ui: {
    incVat: boolean;
    payType: string;
    discPct: number;
    creditDays: number;
    extraDiscPct?: string | number;
    extraDiscAmt?: string | number;
    vatRepName?: string;
    vatRepAddress?: string;
    vatRepIdCard?: string;
  }
) => ({
  customerId: form.customerId ? +form.customerId : "",
  date: form.date || "",
  items: (form.items || [])
    .filter((i) => i && i.productId)
    .map((i) => ({ productId: +(i.productId as string), qty: +(i.qty ?? 0), price: +(i.price ?? 0) })),
  includeVat: ui.incVat,
  payType: ui.payType,
  discPct: ui.discPct,
  creditDays: ui.creditDays,
  extraDiscPct: +(ui.extraDiscPct || 0),
  extraDiscAmt: +(ui.extraDiscAmt || 0),
  useVatRep: !!form.useVatRep,
  vatRepName: ui.vatRepName || "",
  vatRepAddress: ui.vatRepAddress || "",
  vatRepIdCard: ui.vatRepIdCard || "",
  note: form.note || "",
  legacyNum: form.legacyNum || "",
  eventId: form.eventId || "",
  eventPackPurchases: [...(form.eventPackPurchases || [])],
});
```

- [ ] **Step 4: รัน test ให้ผ่าน**

Run: `npm test -- helpers`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/utils/helpers.ts src/utils/helpers.test.ts
git commit -m "feat(sales): draftFromForm maps form to draft record"
```

---

## Task 5: Constant `SO_AUTOSAVE_KEY` + Badge label

**Files:**
- Modify: `src/utils/constants.js` (เพิ่มท้ายไฟล์)
- Modify: `src/components/ui/Badge.jsx:1`

- [ ] **Step 1: เพิ่มค่าคงที่**

เพิ่มท้าย `src/utils/constants.js`:

```js
export const SO_AUTOSAVE_KEY = "so_form_autosave";
```

- [ ] **Step 2: เปลี่ยน label draft เป็นภาษาไทย**

ใน `src/components/ui/Badge.jsx:1` แก้ `draft:"Draft"` เป็น `draft:"ร่าง"` ใน object `LABELS` (ส่วน `M` มี `draft` สีเทาอยู่แล้ว ไม่ต้องแก้)

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: ไม่มี error

- [ ] **Step 4: Commit**

```bash
git add src/utils/constants.js src/components/ui/Badge.jsx
git commit -m "feat(sales): SO_AUTOSAVE_KEY constant + Thai draft badge label"
```

---

## Task 6: ขยาย `MBtns` รองรับปุ่ม secondary + มือถือ

ให้ footer ฟอร์มมี 3 ปุ่มได้ (ยกเลิก / บันทึกร่าง / สร้างใบขาย) โดยไม่กระทบ caller เดิมที่ใช้ 2 ปุ่ม และ wrap ได้บนจอแคบ

**Files:**
- Modify: `src/components/ui/Modal.tsx:22-36`

- [ ] **Step 1: แก้ interface + render ของ MBtns**

แทนที่ `MBtnsProps` และ `MBtns` ใน `src/components/ui/Modal.tsx` ด้วย:

```tsx
interface MBtnsProps {
  onCancel: () => void;
  onSave?: () => void;
  saveLabel?: string;
  disabled?: boolean;
  saveColor?: string;
  secondaryLabel?: string;
  onSecondary?: () => void;
}

export function MBtns({ onCancel, onSave, saveLabel, disabled, saveColor, secondaryLabel, onSecondary }: MBtnsProps) {
  const saveBg = disabled ? "var(--hover2)" : (saveColor || "var(--blue)");
  return <div style={{display:"flex",flexWrap:"wrap",alignItems:"center",gap:10,marginTop:16}}>
    {onSecondary&&<button onClick={onSecondary} style={{padding:"6px 13px",borderRadius:7,border:"1px solid var(--line)",cursor:"pointer",background:"var(--bg2)",color:"var(--text)",fontFamily:"inherit",fontSize:13,fontWeight:500}}>{secondaryLabel||"บันทึกร่าง"}</button>}
    <div style={{display:"flex",gap:10,marginLeft:"auto"}}>
      <button onClick={onCancel} style={{padding:"6px 13px",borderRadius:7,border:"1px solid var(--line)",cursor:"pointer",background:"var(--bg2)",color:"var(--text)",fontFamily:"inherit",fontSize:13,fontWeight:500}}>ยกเลิก</button>
      {onSave&&<button onClick={onSave} disabled={disabled} style={{padding:"6px 13px",borderRadius:7,border:"none",cursor:disabled?"not-allowed":"pointer",background:saveBg,color:disabled?"var(--dim)":"#fff",fontFamily:"inherit",fontSize:13,fontWeight:500,opacity:disabled?0.6:1}}>{saveLabel||"บันทึก"}</button>}
    </div>
  </div>;
}
```

หมายเหตุ: caller เดิมที่ไม่ส่ง `onSecondary` จะได้ layout เหมือนเดิม (ยกเลิก+บันทึกชิดขวาด้วย `marginLeft:"auto"`)

- [ ] **Step 2: typecheck**

Run: `npm run typecheck`
Expected: ไม่มี error

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/Modal.tsx
git commit -m "feat(ui): MBtns optional secondary button + wrap for mobile"
```

---

## Task 7: Autosave (write + restore) + ปุ่ม "เริ่มฟอร์มเปล่า"

**Files:**
- Modify: `src/components/Sales.jsx`

- [ ] **Step 1: เพิ่ม import**

`src/components/Sales.jsx:2` มี `import { IB, DISC_OPTS, CREDIT_OPTS } from "../utils/constants.js";` — เพิ่ม `SO_AUTOSAVE_KEY`:

```js
import { IB, DISC_OPTS, CREDIT_OPTS, SO_AUTOSAVE_KEY } from "../utils/constants.js";
```

`src/components/Sales.jsx:3` ต่อท้าย import จาก `helpers.js` ให้มี `soFormHasContent, parseSoAutosave, resolveSaveSoNum, resolveSaveStatus, draftFromForm` เพิ่มเข้าไปใน list ที่มีอยู่

- [ ] **Step 2: เพิ่ม state `restored` + helper functions**

ใต้บรรทัด `const resetPromoStates=()=>{...};` ([Sales.jsx:36](src/components/Sales.jsx:36)) เพิ่ม (ต้องอยู่ "ใต้" การประกาศ `ef` ที่บรรทัด 26 และเหนือ effect บรรทัด 37):

```js
const[restored,setRestored]=useState(false);
const clearAutosave=()=>{try{localStorage.removeItem(SO_AUTOSAVE_KEY);}catch{}};
const resetFormState=()=>{setForm(ef);setIncVat(true);setPayType("cash");setDiscPct(1);setCreditDays(45);setExtraDiscPct("");setExtraDiscAmt("");resetPromoStates();};
const startBlankForm=()=>{resetFormState();clearAutosave();setRestored(false);};
const openNewSO=()=>{
  setFormErrors([]);
  let saved=null;
  try{saved=parseSoAutosave(localStorage.getItem(SO_AUTOSAVE_KEY));}catch{saved=null;}
  if(saved&&soFormHasContent(saved.form)){
    setForm({...ef,...saved.form});
    setIncVat(saved.incVat!==undefined?saved.incVat:true);
    setPayType(saved.payType||"cash");
    setDiscPct(saved.discPct!=null?saved.discPct:1);
    setCreditDays(saved.creditDays!=null?saved.creditDays:45);
    setExtraDiscPct(saved.extraDiscPct||"");
    setExtraDiscAmt(saved.extraDiscAmt||"");
    resetPromoStates();
    setRestored(true);
  }else{
    resetFormState();
    setRestored(false);
  }
  oM("addSO");
};
```

- [ ] **Step 3: ใช้ `openNewSO` แทนการ reset เดิม 2 จุด**

- [Sales.jsx:37](src/components/Sales.jsx:37) effect quickCreate==="addSO": แทนเนื้อใน `if(...){...}` จากเดิม `setFormErrors([]);setForm(ef);setIncVat(true);setPayType("cash");setDiscPct(1);setCreditDays(45);resetPromoStates();oM("addSO");sh.clearQuickCreate();` เป็น `openNewSO();sh.clearQuickCreate();`
- ปุ่ม "+ สร้างใบขาย" [Sales.jsx:442](src/components/Sales.jsx:442): แทน `onClick={()=>{setFormErrors([]);setForm(ef);...oM("addSO");}}` เป็น `onClick={openNewSO}`

- [ ] **Step 4: เพิ่ม effect autosave (เซฟเฉพาะตอน modal เป็น addSO)**

เพิ่มถัดจาก effect quickSO ([Sales.jsx:38](src/components/Sales.jsx:38)):

```js
useEffect(()=>{
  if(modal!=="addSO")return;
  const t=setTimeout(()=>{
    try{localStorage.setItem(SO_AUTOSAVE_KEY,JSON.stringify({form,incVat,payType,discPct,creditDays,extraDiscPct,extraDiscAmt,savedAt:Date.now()}));}catch{}
  },500);
  return()=>clearTimeout(t);
},[modal,form,incVat,payType,discPct,creditDays,extraDiscPct,extraDiscAmt]);
```

- [ ] **Step 5: เพิ่มแถบ restore ใน renderForm**

ใน `renderForm` ([Sales.jsx:194](src/components/Sales.jsx:194)) หลัง `return (...)` เปิด container ของฟอร์ม ให้เพิ่มแถบนี้ก่อน field แรก (แสดงเฉพาะตอนสร้างใหม่ `soId==null` และ `restored`):

```jsx
{soId==null&&restored&&<div style={{display:"flex",alignItems:"center",justifyContent:"space-between",gap:8,flexWrap:"wrap",background:"var(--blue-bg)",border:"1px solid var(--blue)",borderRadius:8,padding:"8px 12px",marginBottom:12,fontSize:12,color:"var(--blue)"}}>
  <span>กู้ฟอร์มที่ค้างไว้แล้ว</span>
  <button onClick={startBlankForm} style={{padding:"4px 10px",fontSize:11,borderRadius:6,border:"1px solid var(--blue)",background:"transparent",color:"var(--blue)",cursor:"pointer",fontFamily:"inherit"}}>เริ่มฟอร์มเปล่า</button>
</div>}
```

(หา element แรกของ `renderForm` แล้วแทรกไว้บนสุดของเนื้อฟอร์ม — ตรวจให้ JSX root ยังถูกต้อง)

- [ ] **Step 6: ล้าง autosave ตอนปิด modal สร้างใหม่**

หา modal addSO ([Sales.jsx:477](src/components/Sales.jsx:477)) ตอนนี้ `onClose={cM}` — เปลี่ยนเป็น `onClose={()=>{cM();setRestored(false);}}` (ไม่ล้าง autosave ตอนกากบาท เพื่อให้กลับมากู้ได้; autosave จะถูกล้างเมื่อ save สำเร็จ/บันทึกร่าง/เริ่มฟอร์มเปล่าเท่านั้น)

- [ ] **Step 7: typecheck + เริ่ม dev server verify (ปลอดภัย — localStorage เท่านั้น)**

Run: `npm run typecheck` → ไม่มี error
จากนั้น verify ใน preview: เปิดหน้าขาย กดสร้างใบขาย กรอกลูกค้า+สินค้า ปิดด้วยกากบาท เปิดใหม่ → ฟอร์มต้องถูกเติมกลับ + เห็นแถบ "กู้ฟอร์มที่ค้างไว้แล้ว"; กด "เริ่มฟอร์มเปล่า" → ฟอร์มว่าง แถบหาย (การ verify นี้ไม่เขียน DB จึงปลอดภัย)

- [ ] **Step 8: Commit**

```bash
git add src/components/Sales.jsx
git commit -m "feat(sales): autosave SO form to localStorage with restore banner"
```

---

## Task 8: `saveDraft` + ปุ่ม "บันทึกร่าง" ใน footer

**Files:**
- Modify: `src/components/Sales.jsx`

- [ ] **Step 1: เพิ่มฟังก์ชัน `saveDraft`**

เพิ่มเหนือ `const trySubmit=...` ([Sales.jsx:147](src/components/Sales.jsx:147)):

```js
const saveDraft=(soId)=>{
  if(!soFormHasContent(form)){setFormErrors(["ร่างต้องมีลูกค้า หรือสินค้าอย่างน้อย 1 รายการ"]);return;}
  setFormErrors([]);
  const selRep=form.useVatRep&&form.vatRepId?curVatReps.find(r=>r.id===+form.vatRepId):null;
  const draftFields=draftFromForm(form,{incVat,payType,discPct,creditDays,extraDiscPct,extraDiscAmt,vatRepName:selRep?selRep.name:"",vatRepAddress:selRep?selRep.address:"",vatRepIdCard:selRep?selRep.idCard:""});
  if(soId){
    setSales(p=>p.map(s=>s.id===soId?{...s,...draftFields,status:"draft",soNum:"",savedAt:todayStr()}:s));
    setEditSO(null);
  }else{
    setSales(p=>[...p,{id:Date.now()+Math.random(),soNum:"",status:"draft",fromQuote:"",savedAt:todayStr(),...draftFields}]);
  }
  addA("บันทึกร่าง SO","");
  clearAutosave();
  resetPromoStates();
  setRestored(false);
  cM();
};
```

- [ ] **Step 2: เพิ่มปุ่ม "บันทึกร่าง" ใน footer ฟอร์ม**

ที่ `MBtns` ใน `renderForm` ([Sales.jsx:427](src/components/Sales.jsx:427)) เพิ่ม props secondary โดยแสดงเฉพาะตอนสร้างใหม่หรือกำลังแก้ร่าง — ก่อน MBtns ให้คำนวณ:

```js
const showDraftBtn = !soId || editSO?.status==="draft";
```

แล้วแก้ `<MBtns .../>` เพิ่ม 2 props (คงของเดิมไว้):

```jsx
<MBtns onCancel={()=>{setEditSO(null);cM();}} onSave={(hasOver&&!isDropship)?null:()=>trySubmit(soId)} saveLabel={soId?(editSO?.status==="draft"?"สร้างใบขาย":"บันทึก"):"สร้างใบขาย"} secondaryLabel={showDraftBtn?"บันทึกร่าง":undefined} onSecondary={showDraftBtn?()=>saveDraft(soId):undefined}/>
```

(ตอนแก้ร่าง ปุ่มหลัก = "สร้างใบขาย" เพื่อสื่อว่ากดแล้วจะโปรโมตเป็น SO จริง)

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: ไม่มี error

- [ ] **Step 4: Commit**

```bash
git add src/components/Sales.jsx
git commit -m "feat(sales): saveDraft + บันทึกร่าง button"
```

---

## Task 9: Refactor `doSave` รองรับการโปรโมตร่าง

**Files:**
- Modify: `src/components/Sales.jsx:110-142`

- [ ] **Step 1: แทน logic เลข SO (บรรทัด 110-112)**

แทน:
```js
let newSoNum="";
if(!soId){newSoNum=nextDocNum("SO",sales,"soNum");}
else{const oldSO=sales.find(s=>s.id===soId);newSoNum=oldSO?.soNum||"";}
```
ด้วย:
```js
const oldSO=soId?sales.find(s=>s.id===soId):null;
const isPromote=oldSO?.status==="draft";
const newSoNum=resolveSaveSoNum(oldSO,sales);
```

- [ ] **Step 2: แทน branch เซฟ (บรรทัด 137-142)**

แทนทั้งบล็อก `if(soId){...}else{...}` (เดิมที่ [Sales.jsx:137](src/components/Sales.jsx:137)–142) ด้วย:

```js
const status=resolveSaveStatus(oldSO,needsApproval);
if(soId&&!isPromote){
  setSales(p=>p.map(s=>s.id===soId?{...s,...soBase,status}:s));
  const _money=n=>"฿"+fmt(n);const _cust=id=>{const c=contacts.find(x=>x.id===id);return c?cN(c):(id?"#"+id:"—");};const _nameOf=pid=>{const p=products.find(x=>x.id===pid);return p?pN(p):"#"+pid;};
  const _soDefs=[{key:"customerId",label:"ลูกค้า",fmt:_cust},{key:"date",label:"วันที่",fmt:toBE},{key:"payType",label:"การชำระ",fmt:v=>v==="cash"?"เงินสด":v==="credit"?"เครดิต":String(v??"")},{key:"creditDays",label:"เครดิต (วัน)"},{key:"discountAmt",label:"ส่วนลด",fmt:_money},{key:"includeVat",label:"VAT",fmt:v=>v?"รวม":"ไม่รวม"},{key:"vatRepName",label:"ตัวแทน VAT"},{key:"note",label:"หมายเหตุ"}];
  const _changes=oldSO?[...diffFields(oldSO,soBase,_soDefs),...diffLineItems(oldSO.items||[],soBase.items||[],{priceKey:"price",nameOf:_nameOf,fmtMoney:_money})]:[];
  addA("แก้ไข SO",editSO?.soNum||"",_changes);setEditSO(null);
}else if(soId&&isPromote){
  setSales(p=>p.map(s=>s.id===soId?{...s,...soBase,soNum:newSoNum,status,fromQuote:s.fromQuote||""}:s));
  addA("สร้าง SO"+(needsApproval?" (รออนุมัติ)":""),newSoNum);setEditSO(null);
}else{
  setSales(p=>[...p,{id:Date.now()+Math.random(),soNum:newSoNum,status,fromQuote:"",...soBase}]);
  addA("สร้าง SO"+(needsApproval?" (รออนุมัติ)":""),newSoNum);
}
clearAutosave();
setRestored(false);
```

หมายเหตุ:
- บล็อก promo claims (บรรทัด ~114-135) ใช้ `newSoNum` อยู่แล้ว — ตอนโปรโมต `newSoNum` เป็นเลขจริง จึงถูกต้อง ไม่ต้องแก้
- เดิม id ใหม่ใช้ `Date.now()` → เปลี่ยนเป็น `Date.now()+Math.random()` ตาม memory กัน id ชน
- `resetPromoStates();cM();` ท้าย `doSave` ([Sales.jsx:143-144](src/components/Sales.jsx:143)) คงไว้เหมือนเดิม

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: ไม่มี error

- [ ] **Step 4: รัน unit test ทั้งหมด**

Run: `npm test`
Expected: PASS ทั้งหมด (helper ใหม่ + เดิมไม่พัง)

- [ ] **Step 5: Commit**

```bash
git add src/components/Sales.jsx
git commit -m "refactor(sales): doSave uses resolve helpers + draft promote path"
```

---

## Task 10: รายการ SO — แท็บ "ร่าง", แถว draft, exclude จาก stats

**Files:**
- Modify: `src/components/Sales.jsx`

- [ ] **Step 1: exclude draft จาก stats + เพิ่ม draft count**

แทน `stats` useMemo ([Sales.jsx:42](src/components/Sales.jsx:42)) ด้วย:

```js
const stats=useMemo(()=>{const real=mySO.filter(s=>s.status!=="draft");const pend=real.filter(s=>s.status==="pending_delivery").length;const out=real.filter(s=>s.status==="out_for_delivery").length;const comp=real.filter(s=>s.status==="completed").length;const pendApv=real.filter(s=>s.status==="pending_special_approval").length;const draft=mySO.filter(s=>s.status==="draft").length;const totAmt=real.reduce((s,so)=>s+soTot(so)-(so.discountAmt||0),0);return{total:real.length,pend,out,comp,pendApv,draft,totAmt};},[mySO]);
```

- [ ] **Step 2: ซ่อน draft จากแท็บ "ทั้งหมด"**

ใน `filtered` useMemo ([Sales.jsx:29](src/components/Sales.jsx:29)) เพิ่มเงื่อนไขหลัง `if(myCI&&...)`: เพิ่ม `if(fSt==="all"&&so.status==="draft")return false;` (เงื่อนไข `if(fSt!=="all"&&so.status!==fSt)` เดิมจะทำให้แท็บ "ร่าง" โชว์เฉพาะ draft อยู่แล้ว)

- [ ] **Step 3: เพิ่มแท็บกรอง "ร่าง"**

ใน array filter ([Sales.jsx:446](src/components/Sales.jsx:446)) เพิ่ม entry `["draft","ร่าง",stats.draft]` (วางหลัง `["all",...]`):

```js
{[["all","ทั้งหมด",stats.total],["draft","ร่าง",stats.draft],["pending_special_approval","รออนุมัติพิเศษ",stats.pendApv],["pending_delivery","รอจัดส่ง",stats.pend],["out_for_delivery","เตรียมส่ง",stats.out],["completed","ส่งแล้ว",stats.comp]].map(([k,lb,cnt])=>{
```

และในบรรทัดสี ([Sales.jsx:447](src/components/Sales.jsx:447)) เพิ่ม `k==="draft"` ให้ใช้สีเทา — แก้ `clr` และ `bg`:

```js
const active=fSt===k;const clr=k==="pending_delivery"?"var(--orange)":k==="out_for_delivery"?"var(--blue)":k==="completed"?"var(--green)":k==="pending_special_approval"?"var(--purple)":"var(--dim)";const bg=k==="pending_delivery"?"rgba(255,149,0,0.14)":k==="out_for_delivery"?"var(--blue-bg)":k==="completed"?"rgba(52,199,89,0.12)":k==="pending_special_approval"?"rgba(175,82,222,0.12)":k==="draft"?"var(--hover)":"var(--bg)";
```

(`clr` สำหรับ draft ตกไป default `var(--dim)` อยู่แล้ว เพิ่มเฉพาะ `bg`)

- [ ] **Step 4: แถว draft — แสดงเลขเป็น "(ร่าง)" + ปุ่มแก้ไขร่าง**

- soNum cell ([Sales.jsx:455](src/components/Sales.jsx:455)): เปลี่ยน `{so.soNum}` เป็น `{so.soNum||"(ร่าง)"}` และให้คลิก draft เปิดแก้ไขแทน view:
  เปลี่ยน `onClick={()=>{setViewSO(so);oM("viewSO");}}` เป็น `onClick={()=>{if(so.status==="draft"){openEdit(so);}else{setViewSO(so);oM("viewSO");}}}`
- ปุ่มแก้ไข ([Sales.jsx:470](src/components/Sales.jsx:470)): เพิ่มเงื่อนไข draft ให้โชว์ปุ่ม "แก้ไขร่าง". หลังบรรทัด 470 เพิ่ม:
  ```jsx
  {ed&&so.status==="draft"&&<button onClick={()=>openEdit(so)} style={{padding:"4px 10px",fontSize:11,borderRadius:6,border:"1px solid var(--dim)",background:"var(--hover)",color:"var(--dim)",cursor:"pointer",marginRight:4,fontFamily:"inherit"}}>แก้ไขร่าง</button>}
  ```
  (ปุ่ม "ลบ" บรรทัด 473 โชว์อยู่แล้วเพราะ draft ไม่มี linkedPO; "จัดส่ง"/"อนุมัติ" ไม่โชว์เพราะ status ไม่ตรงเงื่อนไข)

- [ ] **Step 5: แก้ title modal แก้ไขให้รองรับร่าง**

modal editSO ([Sales.jsx:478](src/components/Sales.jsx:478)): เปลี่ยน `title={"แก้ไข — "+editSO.soNum}` เป็น `title={editSO.status==="draft"?"แก้ไขร่าง":"แก้ไข — "+editSO.soNum}`

- [ ] **Step 6: typecheck + รัน test**

Run: `npm run typecheck` แล้ว `npm test`
Expected: ไม่มี error, test PASS ทั้งหมด

- [ ] **Step 7: Commit**

```bash
git add src/components/Sales.jsx
git commit -m "feat(sales): ร่าง filter tab, draft rows, exclude drafts from stats"
```

---

## Task 11: ไล่ leak ที่ aggregate sales แบบไม่กรองสถานะ (นอก Sales.jsx)

**Files:**
- ตรวจ (อาจ Modify): consumer ของ `sales` ที่ count/sum รวมทุกสถานะ

- [ ] **Step 1: grep หา aggregation ที่ไม่กรองสถานะ**

```bash
grep -rn "sales.reduce\|sales.length\|sales.filter" src --include=*.jsx --include=*.js --include=*.ts | grep -v "status"
```

ไล่ดูแต่ละจุด: ถ้าเป็นการนับ/รวมยอดรวมทุกสถานะ (เช่น Dashboard total sales, AISOBot context, BackupManager export count) ให้ประเมินว่าควร exclude draft ไหม

- [ ] **Step 2: เพิ่ม filter `status!=="draft"` เฉพาะจุดที่รวมยอด/นับ "ทั้งหมด"**

สำหรับแต่ละจุดที่ยืนยันว่าเป็นยอดรวมแสดงผลธุรกิจ (ไม่ใช่ debug/backup) เพิ่ม `.filter(s=>s.status!=="draft")` ก่อน reduce/length

หมายเหตุการตัดสิน:
- **Backup/export (BackupManager)**: draft ควรถูก backup ด้วย (เป็นข้อมูลจริง) → **ไม่ต้อง** filter
- **GlobalSearch**: ค้นด้วย soNum, draft soNum ว่าง → ไม่โผล่อยู่แล้ว → ไม่ต้องแก้
- **รายงาน/Dashboard ยอดขาย**: ถ้านับรวมทุกสถานะ → filter draft ออก
- consumer ที่กรอง `status==="completed"`/สถานะเฉพาะอยู่แล้ว → ไม่ต้องแก้

- [ ] **Step 3: typecheck + test**

Run: `npm run typecheck` แล้ว `npm test`
Expected: ไม่มี error, PASS

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "fix(sales): exclude draft SOs from business-total aggregations"
```

---

## Task 12: Verification รวม + manual (DB write ต้องขออนุญาต)

- [ ] **Step 1: typecheck + test ครบ**

Run: `npm run typecheck && npm test`
Expected: ไม่มี error, test ทั้งหมด PASS

- [ ] **Step 2: build**

Run: `npm run build`
Expected: build สำเร็จ ไม่มี error

- [ ] **Step 3: manual verify (อ่านอย่างเดียว — ปลอดภัย)**

ใน preview: หน้าขายต้องมีแท็บ "ร่าง (0)"; เปิดฟอร์มสร้างใหม่ กรอกค้าง ปิด เปิดใหม่ → กู้คืนได้ + แถบ restore; footer มีปุ่ม ยกเลิก/บันทึกร่าง/สร้างใบขาย และ wrap สวยบนจอแคบ (preview_resize)

- [ ] **Step 4: manual verify ที่เขียน DB — ขออนุญาตผู้ใช้ก่อน**

**หยุดถามผู้ใช้ก่อนทำขั้นนี้** (เขียน prod Supabase): ทดสอบ บันทึกร่าง → เห็นในแท็บ "ร่าง" เลข "(ร่าง)" badge เทา; เปิดร่าง แก้ไข กด "สร้างใบขาย" → ได้เลข SO จริง สถานะ รอจัดส่ง/รออนุมัติ และร่างเดิมหายไป (id เดิมถูกแปลงเป็น SO จริง ไม่เหลือร่างค้าง); ยืนยันยอดรวม/รายงานไม่นับร่าง

- [ ] **Step 5: อัปเดต memory (ถ้าควร)**

ถ้าระหว่างทางเจอพฤติกรรมไม่ชัดที่ควรจำ ให้บันทึกลง memory ตามแนวทางที่มี

---

## Self-Review Notes

- **Spec coverage:** ส่วน A (autosave) → Task 1,2,5,7; ส่วน B (draft) → Task 3,4,5,6,8,9,10; Mobile → Task 6 (MBtns wrap) + Task 12 step3; audit leak → Task 11. ครบทุกหัวข้อสเปก
- **Type consistency:** ชื่อ helper สอดคล้องทุก task (`soFormHasContent, parseSoAutosave, resolveSaveSoNum, resolveSaveStatus, draftFromForm, clearAutosave, openNewSO, startBlankForm, resetFormState, saveDraft`); draft record ใช้ฟิลด์เดียวกับที่ `openEdit` อ่าน
- **id:** record ใหม่ทุกที่ใช้ `Date.now()+Math.random()`
- **Behavior preservation:** การแก้ SO จริงเดิม/สร้างใหม่ใน `doSave` ให้ผลเท่าเดิม (resolveSaveStatus = keepStatus logic เดิม), MBtns caller เดิมไม่กระทบ
