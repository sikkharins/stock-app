# คัดลอกเป็น .env แล้วเติมค่าจริง (รันจริง: node --env-file=.env server.js)
# เว้น CAM_IP ว่าง = mock mode (เทสได้โดยไม่มีกล้อง)
CAM_IP=
CAM_USER=
CAM_PASS=
ONVIF_PORT=2020
RTSP_PATH=stream1
RELAY_PORT=8765
SETTLE_MS=2500
RELAY_MOCK=
